-- Seed data for Us MVP (Phase 2 Task: T016)
-- This file is intentionally minimal for MVP
-- Populated with test data during development as needed

-- Default seed data can be added here
-- Example structure:
-- INSERT INTO users (email, password_hash, display_name) VALUES
--   ('alice@example.com', crypt('password123', gen_salt('bf')), 'Alice'),
--   ('bob@example.com', crypt('password123', gen_salt('bf')), 'Bob');
